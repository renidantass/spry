from __PACKAGE_NAME__.data import AppDbContext
from __PACKAGE_NAME__.models import Todo


def seed(db: AppDbContext) -> None:
    if db.todos.count() > 0:
        return

    db.todos.add(Todo(
        title="Publicar o template MVC",
        description="Validar o fluxo server-side com UI inspirada no shadcn.",
        done=False,
    ))
    db.todos.add(Todo(
        title="Modelar o primeiro modulo",
        description="Comecar pelo bounded context mais simples.",
        done=False,
    ))
    db.todos.add(Todo(
        title="Documentar o onboarding",
        description="Explicar scaffold, run, watch e seed.",
        done=True,
    ))
