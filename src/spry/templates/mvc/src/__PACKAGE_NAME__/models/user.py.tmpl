from dataclasses import dataclass

from spry import column, key


@dataclass(slots=True)
class User:
    id: int | None = key()
    username: str = column(unique=True, default="")
    display_name: str = ""
    password_hash: str = ""
    roles: str = "member"
