name: Deploy

on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pip install spry-core
      - run: python -m unittest discover -s tests -v

  build:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build Docker image
        run: docker build -t __PACKAGE_NAME__:latest .
      # Push to your registry
      # - name: Push to registry
      #   run: |
      #     docker tag __PACKAGE_NAME__:latest registry.example.com/__PACKAGE_NAME__:latest
      #     docker push registry.example.com/__PACKAGE_NAME__:latest
